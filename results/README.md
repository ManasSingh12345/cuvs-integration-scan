# Results

Snapshot from `cuvs_scan.py`, 2026-07-05.

- **`cuvs.db`** — SQLite database, all 413 evidence rows (table `hits`).
- **`cuvs_high_confidence.csv`** — 140 HIGH-precision, non-fork repos; one row per repo at its highest tier.
- **`cuvs_high_confidence_strict.csv`** — the 114 of those with a real cuVS symbol (bare-`CAGRA` noise removed).
- **`cuvs_dashboard.html`** — interactive dashboard of all 219 repos; open in a browser.
